vcenter_build: | 
  <speak>
      vCenter Server is running {{ version }} using build: <say-as interpret-as="digits">{{build}}</say-as><say-as interpret-as="interjection">booya</say-as>
  </speak>


capacity_report: |
  <speak>
      My vCenter environment is currently running vCenter {{ version }} with build <say-as interpret-as="digits">{{build}}</say-as> and my appliance deployment health 
      is {{ health }}. My vCenter has been running for {{ uptime }} hours. 
      I currently have {{ cpu }} CPU and {{ memory }} GB of memory provisioned across {{ clustercount }} clusters. This represents {{ vmcount }} of which {{ poweredon }}
      are powered on.
  </speak>
